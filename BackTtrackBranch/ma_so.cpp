#include<bits/stdc++.h>

using namespace std;

void xuat(int a[], int n)
{
    for(int i = 1; i <= n; i++)
    {
        cout << char(64 + a[i]);
    }
    for(int i = 0; i < )
}

void hoan_vi(int a[], int n)
{
    int i = n - 1;
    while(i > 0 && a[i] > a[i ])
}

int main()
{
    int n;
    cin >> n;
    return 0;
}